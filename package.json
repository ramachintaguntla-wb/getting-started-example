{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.0.1"
  },
  "scripts": {
    "release": "lerna exec --concurrency 1 -- npx semantic-release -e semantic-release-monorepo"
  }
}
